# Example environment variables for local development
# Copy to .env.local and fill values (do NOT commit .env.local)

# Base URL of your backend (no trailing slash)
JEDHA_BACKEND_URL=http://localhost:8000

# API key to communicate between frontend server (proxy) and backend
JEDHA_BACKEND_API_KEY=replace_with_backend_api_key
